{
  "name": "novel-server-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --host",
    "build": "vite build",
    "preview": "vite preview --port 5173 --host",
    "test": "vitest run"
  },
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "normalize.css": "^8.0.1",
    "sass": "^1.60.0",
    "svelte": "^5.46.4",
    "vite": "^8.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
